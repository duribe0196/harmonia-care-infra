{
    "name": "harmonia-care-infra",
    "main": "index.ts",
    "devDependencies": {
        "@types/node": "^18",
        "prettier": "^3.2.5",
        "typescript": "^5.0.0"
    },
    "scripts": {
        "pulumi:dev:up": "export AWS_PROFILE=pulumi && export PULUMI_ENV=dev && pulumi refresh && pulumi up",
        "pulumi:dev:destroy": "export AWS_PROFILE=pulumi && export PULUMI_ENV=dev && pulumi refresh && pulumi destroy"
    },
    "dependencies": {
        "@pulumi/aws": "^6.0.0",
        "@pulumi/aws-apigateway": "^2.4.0",
        "@pulumi/awsx": "^2.0.2",
        "@pulumi/mongodbatlas": "^3.15.0",
        "@pulumi/pulumi": "^3.113.0"
    }
}
